# CAN协议说明

这份说明的目的，是先把系统里的报文关系讲清楚，方便快速看懂源码和调试记录。

## 报文分工

| CAN ID | 方向 | 用途 |
| --- | --- | --- |
| `0x100` | Master -> Node | 控制命令，包含目标转速、使能位、序列号 |
| `0x200` | Node -> Master | 状态反馈，包含实际转速、PWM、状态、故障码 |
| `0x300` | Node -> Master | 心跳报文 |
| `0x400` | Node -> Master | 节点注册报文 |
| `0x500` | 双向 | 应答报文 |

## 报文组织思路

- 使用标准帧 11 位 ID
- 控制、状态、心跳、注册、应答分开管理
- 通过应答和序列号，避免单次报文丢失后无法追踪
- 通过注册和心跳，区分“已经在线”与“已经收到控制命令”这两件事

## 推荐的运行顺序

1. 节点上电并完成初始化
2. 节点发送注册报文
3. 主控回送应答
4. 节点进入在线状态后再发心跳和状态
5. 主控按需下发控制命令
6. 节点根据控制命令更新 PI 和 PWM 输出

## 调试时最容易踩的点

- 主控和节点的波特率、位时序必须一致
- 注册还没完成时就发送状态或心跳，容易把调试过程搅乱
- 采样点太早、位时间量子太少，会让 CAN 发送/接收变得不稳定
- 逻辑上要先区分“在线”与“有状态”，不要把两者混在一起

## 说明

字段级细节以源码中的协议解析实现为准。  
这个文件主要负责把“报文之间是什么关系”讲清楚。
